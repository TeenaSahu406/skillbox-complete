import {useEffect,useState} from 'react';import {useParams} from 'react-router-dom';import Navbar from '../components/Navbar';import Footer from '../components/Footer';import {supabase} from '../lib/supabase';import type {Profile} from '../types/db';
export default function ProfilePublic(){const{id}=useParams(),[p,setP]=useState<Profile|null>(null);useEffect(()=>{if(id)supabase.from('candidate_public_profiles').select('*').eq('id',id).maybeSingle().then(({data})=>setP(data as Profile|null))},[id]);return <><Navbar/><main className="page"><div className="wrap narrow">{p?<div className="card profile"><small>CANDIDATE PROFILE</small><h1>{p.full_name}</h1><h3>{p.headline}</h3><p>{p.location}</p><h2>About</h2><div className="prose">{p.bio||'No bio added yet.'}</div><div className="video">▶<small>INTRO VIDEO</small></div><p className="muted">Resume and private files are accessed through authorized storage policies.</p></div>:<div className="empty">Candidate not found.</div>}</div></main><Footer/></>}
